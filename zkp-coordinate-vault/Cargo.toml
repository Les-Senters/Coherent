[package]
name = "zkp-coordinate-vault"
version = "1.0.0"
edition = "2021"
description = "Cryptographic session gate managing zero-knowledge token properties."
license = "MIT OR Apache-2.0"

[dependencies]
thiserror = { version = "1.0" }
