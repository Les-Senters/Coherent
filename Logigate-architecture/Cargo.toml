[package]
name = "logigate-architecture"
version = "1.0.0"
edition = "2021"
description = "Zero-trust memory-wiping middleware sandbox engine."
license = "MIT OR Apache-2.0"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
